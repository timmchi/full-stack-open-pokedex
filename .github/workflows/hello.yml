name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello world
        run: |
          echo "Hello World!"
      - name: Run date
        run: |
          date
      - name: Run ls -l
        run: |
          ls -l
